import { heading } from "../fonts";

export default function ProjectSection() {
    return (
        <div className="p-6">
            <h2 className={`${heading.className} text-xl sm:text-2xl md:text-4xl`}>Recent Projects</h2>
            <div>
                
            </div>
        </div>
    );
}